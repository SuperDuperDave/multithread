# Release acceptance matrix

This is the retained initial-release acceptance checkpoint, published under the
Agent Relay name. Its counts, scope and state descriptions are historical
evidence, not a live report for subsequent Multithread releases. See the
[current overview](../../README.md), [peer evidence](../PEER.md#coordination-and-verification-scope)
and each selected release for later capabilities and verification.

Evidence applies only to the behavior and environment exercised. The bounded
[native workflow](../PROVIDERS.md#bounded-native-workflow) complements the local
no-account checks. [Hosted CI](../CI.md#hosted-result) records its exact commit;
consult [v0.1.0](https://github.com/SuperDuperDave/multithread/releases/tag/v0.1.0) for package and onboarding results.

| ID | Required outcome | Acceptance evidence | State |
|---|---|---|---|
| R01 | Known source and rights | Immutable closed manifest, delta review, ownership/notice inventory | Scoped provenance/dependency/notice inventory is recorded. The operator approved MIT licensing, copyright (c) 2026 David Jones and code publication. This records user attestation, not independent verification of ownership or outside obligations. Selected source semantics, public metadata and the fresh-history export are reviewed; preserve inherited provenance/notices in later artifacts |
| R02 | Independent project | Exact root/common-dir proof; no original application's state/history/config imported | Local Git boundary verified |
| R03 | Safe install and enrollment | Fresh install; refusal before mutation for unregistered/foreign/ambiguous workspaces; rollback/uninstall tests | Public install/rollback, source-absent init/ledger, damaged-launcher recovery and disable pass. Verified code uninstall/retry/reinstall preserves an actual ledger, active claim, account enrollment and unrelated files; non-code recovery metadata deliberately remains. Invocation-only provider arguments avoid persistent settings; actual Codex restart without flags removes the contribution. Approved external-bundle recovery also preserves actual ledger bytes/work intent/claim with zero valid retained candidates and source-absent reopen; partial same-digest destinations remain preserved and blocked. Explicit same-object rebind now preserves actual pending handoffs/claims and physical ledger objects through installed source-absent moves and return moves; exact-successor guards and independent interruption/substitution/refusal tests pass. Native hook trust, context and the bounded authenticated workflow are verified on the documented profile. Copy/backup/cross-filesystem migration remain unproven |
| R04 | Durable ledger and claims | SQL guard, concurrent contention, abrupt exit/reopen, no-TTL, exact-holder release/break tests | Inherited and internal installed contention/crash witnesses pass; public source-absent flow now proves competing claims, wrong-session refusal, exact release and shared linked-worktree identity. The bounded native workflow also verifies occupied-resource refusal, original-holder release and the reviewer claim lifecycle |
| R05 | Correct handoff and decisions | Duplicate exact retry, conflicting retry, pending until consumed, atomic response+ACK, immutable artifact checks | Inherited/internal decision atomicity/crash tests plus public decision response/ACK and exact retry pass against real SQLite. The bounded native workflow verifies inspection of exact artifacts, deliberate acknowledgements, a return handoff and duplicate-wake handling with sequence-only reviewer wakes and explicit author task instructions |
| R06 | Useful agent behavior | Installable minimal contracts; real Codex/Claude code/review cycle with effective runtime proof | Installed contracts, ordered hooks and invocation-only configuration pass local checks. The bounded authenticated run verifies Codex 0.153.4 author/Claude 2.1.267 reviewer behavior, trusted hook context and the same exact native sessions across phases. The models authored the fix/review; a controller independently checked their bytes/tests and created scoped Git commits. Historical discovery/startup receipts retain their original credential-free scope |
| R07 | Honest wake/recovery | Lost/duplicate wake and process restart tests; transport cannot grant authority or silently consume work | No-account scenario stops the producer before notification, rediscovers pending work in a fresh process and proves repeated pending reads append no events/ACK. The native run verifies sequence-only manual/controller reviewer wakes and duplicate handling in the exact retained reviewer session; author phases receive explicit task instructions. Relay does not ship automatic message forwarding or unattended scheduling |
| R08 | Reproducible demo | No-account deterministic harness plus real integration demo; independently inspected outcomes | Runnable public-installed no-account demo passes twice with identical source-bound reports, real Git/test artifacts and direct SQLite ACK linkage; an independently authored broken-fix negative cannot report success. The separate bounded native coding/review round trip passes on the documented versions/profile; it is not a deterministic no-account harness |
| R09 | Supported-environment claims | Fresh Linux/WSL install and separate profile proof; CI matrix and explicit unsupported cases | WSL/ext4 and fresh rootless-account witnesses pass; strict local acceptance executes all 327 discovered tests with zero skips/expected failures. SUPPORT describes actual prerequisites and no permissive fallback. The pinned GitHub-hosted workflow passes its recorded public commit on ubuntu-24.04 with an explicitly entered, temporary enforced `relay-ci` AppArmor compatibility profile. Its broad allowances apply to the CI process tree after host privileges are dropped; the preflight verifies ordinary IDs, zero host capabilities, no-new-privs and two nested namespace levels under that profile before all 327 strict tests, demo and selection checks. AppArmor stays enabled globally; developer machines are unchanged. This covers that configured runner only; additional profiles remain unverified |
| R10 | Privacy-safe publication | Positive file manifest, metadata/distribution scan, seeded negative controls, no live coordination data | The exact 56-file candidate passed snapshot checks with 24 reviewed exceptions, private fresh-history filesystem/metadata/object closure and source-absent installation/package/286-test/demo execution. Eight private export controls supplement 12 default audit controls. A later 66-file candidate passed separate private fresh-history verification with 327 strict tests and the scripted demo. Current selection is 67 files with 25 reviewed exact exceptions after adding LICENSE; the frozen 56/66-file receipts remain historical, not validation of later license/status changes. The operator authorized publishing reviewed code, not exporting existing development history; historical blobs remain unscanned. The current selected source, deliberate predecessor exclusions, approved public metadata and exact fresh-history export passed their separate reviews; the public export reran all 327 strict tests and isolated install/package/demo checks. Final archive/download results belong to the tagged release. Recording artifacts remain outside the selection pending the separate user-review gate |
| R11 | Quality and maintainability | Automated checks, adversarial review resolved, architecture/security/troubleshooting docs, limitations | Architecture, CI and support/troubleshooting boundaries documented; strict local 327-test gate and independent runner/workflow review pass. The bounded native workflow and recorded hosted CI pass. Public Issues and private vulnerability reporting are enabled and linked in SUPPORT; consult the release for package-specific results |
| R12 | Public repository and onboarding | Approved identity/license; actual public URL; green CI; fresh anonymous clone quickstart | Public source is [SuperDuperDave/multithread](https://github.com/SuperDuperDave/multithread) with approved MIT licensing and David Jones credit. The recorded hosted CI passes; consult [v0.1.0](https://github.com/SuperDuperDave/multithread/releases/tag/v0.1.0) for actual anonymous-clone and downloaded-package onboarding results |
| R13 | Portfolio deliverable | Short working demo and accurate human/AI contribution and engineering narrative | Executable no-account story, bounded native demonstration and architecture/tradeoff walkthrough exist. README credits David Jones with AI assistance and describes Codex implementation/testing/release work and Claude native review. The tagged release is linked above for package/onboarding evidence. Every video, cast, transcript and recording receipt stays private until separate user review before public distribution; code-publication approval does not release those artifacts |
